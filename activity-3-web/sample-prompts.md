# 活動三 Prompt（講者投影用）

## 主推 Prompt：生成一個可預覽的靜態小工具

```
請幫我用純 HTML + CSS + Vanilla JS 寫一個教學用小網站，功能是「功課命名器」。

要求：
1. 輸出到 activity-3-web/output/ 下面，包含：index.html、styles.css、app.js
2. UI 要現代、清晰、mobile responsive（兩欄 → 手機一欄）
3. 表單欄位：學生姓名、科目、原檔名、前綴（預設【功課】）
4. 按「生成」後顯示建議檔名：{prefix}_{student}_{subject}.{ext}
5. 提供「複製到剪貼簿」按鈕（成功/失敗提示）
6. 所有文字用香港繁中書面語
7. 完成後話我點樣用瀏覽器打開 index.html 預覽

提示：課後可將 output 複製到 NAS 的 _web folder，經 teacher.chw.edu.hk 發佈（見 handouts/07-static-site-publish.md）。
```

## 變體：變成「會議紀錄範本產生器」（呼應活動一）

```
請做一個靜態小網站：貼入會議基本資料（日期、地點、出席者），
一鍵生成 minutes-template.md 的骨架，並可複製。
輸出到 activity-3-web/output/。
全部用香港繁中書面語。
```

## 精簡版（時間唔夠）

```
請參考 @activity-3-web/starter/ 的功課命名器，
幫我生成一個改咗色系同標題的版本，輸出到 activity-3-web/output/。
完成後話我點 preview。
```

## 變體：加校徽及 school branding

```
請將 output 版本加入校徽位置（先用 placeholder），
並將色系改為深藍 + 紫，footer 顯示學校名稱。
```
